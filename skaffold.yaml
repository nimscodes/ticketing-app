apiVersion: skaffold/v4beta6
kind: Config
manifests:
  rawYaml: 
    - ./infra/k8s/*
build:
  # local:
  #   push: false
  googleCloudBuild:
    projectId: ticketing-dev-393619
  artifacts:
    - image: us.gcr.io/ticketing-dev-393619/auth
      context: auth
      docker:
        dockerfile: Dockerfile
      sync:
        manual:
          - src: "src/**/*.ts"
            dest: .
